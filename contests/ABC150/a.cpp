#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i=0; i < (int)(n); i++)
#define rep2(i, s, n) for (int i = (s); i < (int)(n); i++)

int main(){
  int k,x;
  cin >> k >> x;
  if(k*500 >= x) cout << "Yes" << endl;
  else cout << "No" << endl;
  return 0;
}
